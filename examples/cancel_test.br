task def void do_work():
    wait(Duration.fromMillis(500))

task def void main() throws:
    let f = task: do_work()
    f.cancel()
    try:
        f.wait
        print "completed"
    catch:
        print "cancelled: {error}"
