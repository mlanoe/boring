// Generated by boring build --target cuda.
// Compile to PTX with: nvcc --ptx kernels/main.cu -o $OUT_DIR/main.ptx

#include <cuda_runtime.h>
#include <stdint.h>

extern "C" {

// ─── kernel Saxpy ───

__global__ void Saxpy_kernel(const double alpha, const double* x, double* y) {
    const auto i = (threadIdx.x + (blockIdx.x * blockDim.x));
    y[i] = ((alpha * x[i]) + y[i]);
}

}  // extern "C"
