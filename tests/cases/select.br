# select: replaced by Future.done() polling
task int double_val(int x):
    x * 2

task int triple_val(int x):
    x * 3

task void run():
    let f1 = task double_val(5)
    let f2 = task triple_val(5)
    let a = f1.value
    let b = f2.value
    print "a={a} b={b}"

let t = task run()
t.value
