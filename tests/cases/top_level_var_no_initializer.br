# `var int x` with no initializer at top level used to crash the transpiler
# with `unwrap()` on `None` in `pre_scan` (audit finding #5) — the local
# (in-function) equivalent was already guarded in emit_let.rs, but pre_scan
# runs on top-level items first and had no such guard.
var int x
x = 5
print x
